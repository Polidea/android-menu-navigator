package pl.polidea.navigator.ui;

/**
 * Handles information that level in breadcrumbe should be changed to specified
 * one.
 */
public interface OnLevelChangeListener {
    void changeLevel(int toLevel);
}
